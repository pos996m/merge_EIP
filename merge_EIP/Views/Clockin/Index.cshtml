
@{
    ViewBag.Title = "打卡";
    if (Session["ID"] == null)
    {
        Response.Redirect("~/Login/Logout");
    }
}

<h2>Index</h2>

員工編號: @Session["ID"]
員工名稱: @Session["Name"]
員工部門: @Session["Dep"]
員工職位: @Session["Pos"]
職位編號: @Session["PosID"]
